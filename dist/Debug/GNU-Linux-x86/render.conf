render 
{ 
	# Nom du fichier scene que l'on veut traiter
	scene_file = "scene_3_spheres.txt"
	# Nom de la caméra utilisée pour le rendu
	camera = "Camera 1"
	# Type de fichier de sortie (pour l'instant, uniquement PPM)
	output_format = "PPMB" 
	# Format (largeur puis hauteur) de l'image de sortie
	# en nombre de pixels
	output_size = 640 480
}
